import PostList from './PostList'

const Home = (props) => (
    <div>
      <h1>HomePage</h1>
      {/* <button onClick={()=>props.changePages('about')}>Go to about</button> */}
    </div>
  );

  export default Home;
